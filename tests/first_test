''' 
If the previous number is even, the next number is one half of the previous number. 
If the previous number is odd and larger than 1, the next number is 3 times the previous number plus 1. 
If the previous number is 1 then stop and return the list.
'''

import math

n = 3

def isinstance(n, int):
    if (type(n) == int):
        return True 

def niceintegers(n): 
    # if 'n' is positive generate a list 
    new_list = []

    if isinstance(n, int):
        new_list.append(n)
        print("before loop: ", new_list)
        
        for i in range(n):
            if n % 2 == 0: # first factor 
                new_list.append(n/2)

            if i % 2 == 0 and n > 1: # second factor 
                new_list.append(n*3+1)
            
            if n == 1: 
                return new_list
            return new_list
     

# If the input is not a positive integer, 
# the function should return an empty list

    

def main():
    print("after loop:", niceintegers(n))


if __name__ == "__main__":
    main()